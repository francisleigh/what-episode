{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "bun run fetch:catalogue:running && next build",
  "crons": [
    {
      "path": "/api/cron/refresh-catalogue",
      "schedule": "0 6 * * 1"
    }
  ]
}
